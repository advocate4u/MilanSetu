# Optional. When omitted during `npm run dev`, Vite proxies /api to https://localhost:7001.
# For a deployed frontend, set this to the public HTTPS origin of MilanSetu.Api, without a trailing slash.
VITE_API_BASE_URL=https://api.example.com
