import { useEffect, useState } from 'react'
import { getSafety } from './safetyStatusApi'

type Safety={openReports:number;blockedProfiles:number;unreadSafetyNotifications:number;reportsLast30Days:number;protections:string[]}
export default function SafetyPanel(){const[s,setS]=useState<Safety|null>(null),[error,setError]=useState('');const load=async()=>{try{setS(await getSafety());setError('')}catch(e){setError(e instanceof Error?e.message:'Unable to load safety status.')}};useEffect(()=>{void load();const on=()=>void load();window.addEventListener('milansetu:notifications-changed',on);return()=>window.removeEventListener('milansetu:notifications-changed',on)},[]);return <section className="profile-editor" aria-label="Safety status"><div className="profile-editor-head"><div><p className="eyebrow">SAFETY</p><h2>Your safety center</h2><p>Review your protections and reporting activity.</p></div><button className="secondary-button" onClick={()=>void load()}>Refresh</button></div>{error&&<p role="alert">{error}</p>}{s&&<><div className="profile-fields"><div><strong>{s.openReports}</strong><small>Open reports</small></div><div><strong>{s.blockedProfiles}</strong><small>Blocked profiles</small></div><div><strong>{s.unreadSafetyNotifications}</strong><small>Unread safety notices</small></div><div><strong>{s.reportsLast30Days}</strong><small>Reports in 30 days</small></div></div><ul>{s.protections.map(x=><li key={x}>{x}</li>)}</ul></>}</section>}
